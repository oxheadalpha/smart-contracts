// import { bootstrap, TestTz } from './bootstrap-sandbox';
import { $log } from '@tsed/logger';

describe('hello', () => {
  test('test', () => {
    // $log.info('test');
    console.debug('HELLO');
  });
});
